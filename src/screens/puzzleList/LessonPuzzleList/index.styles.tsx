import { View } from 'react-native';
import styled from 'styled-components';

export const PuzzleListContainer = styled(View)`
  flex: 1;
`;

export const CardsContainer = styled(View)`
  margin: 10px 0;
  gap: 15px;
`;
